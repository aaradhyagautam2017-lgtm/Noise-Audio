{
  "name": "noise-audio-dashboard",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "description": "No app lives here. This exists only so Vercel treats middleware.js and api/*.js as ES modules — the dashboard itself is generated by scripts/build_dashboard.py from repository state."
}
